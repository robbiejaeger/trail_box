<h1>Create New Category</h1>

<%= form_for [:admin, @category] do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <%= f.submit %>
<% end %>

<%= link_to "Back to All Categories", admin_categories_path %>
